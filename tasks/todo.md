- [x] Initialize a git repository in this project
- [x] Add a `.gitignore` for Swift/Xcode build artifacts and local files
- [x] Add a `README.md` with a short project overview
- [x] Create the initial commit on `main`
- [x] Create GitHub repo `koenbok/pingbar` (public) with `gh`
- [x] Push local `main` to `origin`

## Review
- Git repo initialized on `main`.
- `.gitignore` excludes Swift/Xcode and local build artifacts.
- `README.md` added with structure and build command.
- Initial commit created and pushed.
