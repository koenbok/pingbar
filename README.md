# PingBar

PingBar is a small macOS Swift app with a C-based ping helper.

## Project Structure

- `Sources/PingBarApp`: Swift app entry point
- `Sources/CPing`: C helper and headers

## Build

```bash
swift build -c release
```
